const dashboardConfig = {
  dropSection: '#drop-section',
  blockListSection: '#block-list-section',
  registeredBlock: '.form-field',
  previewButton: '#preview',
  updateOrderButton: '#update-order',
};

const previewerConfig = {
  dropSection: '#drop-section',
  previewSection: '#preview-section',
  previewButton: '#preview',
  backToEditButton: '#back-to-edit',
  blockListSection: '.form-field-container',
  updatebutton : "#update-order"
};

export { dashboardConfig, previewerConfig };
